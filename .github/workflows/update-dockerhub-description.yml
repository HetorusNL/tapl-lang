name: Update Dockerhub Description

on:
  push:
    branches:
      - master
    paths:
      - README.md
      - .github/workflows/dockerhub-description.yml
  workflow_dispatch:

jobs:
  update-docker-hub-description:
    runs-on: ubuntu-latest

    steps:
      # checkout the repo to $GITHUB_WORKSPACE
      - uses: actions/checkout@v4

      # update the docker hub description and overview for tapl-compyler
      - name: Dockerhub description and overview update
        uses: peter-evans/dockerhub-description@v5
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}
          repository: ${{ secrets.DOCKERHUB_USERNAME }}/tapl-compyler
          short-description: ${{ github.event.repository.description }}

      # update the docker hub description and overview for tapl-compyler-full
      - name: Dockerhub description and overview update
        uses: peter-evans/dockerhub-description@v5
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}
          repository: ${{ secrets.DOCKERHUB_USERNAME }}/tapl-compyler-full
          short-description: ${{ github.event.repository.description }}

      # update the docker hub description and overview for tapl-compyler-small
      - name: Dockerhub description and overview update
        uses: peter-evans/dockerhub-description@v5
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}
          repository: ${{ secrets.DOCKERHUB_USERNAME }}/tapl-compyler-small
          short-description: ${{ github.event.repository.description }}
